"""OpenHands-native Clarify runtime.

This package intentionally lives inside the OpenHands checkout.  It must not
import the parent repository's legacy ``clarify`` package, so the OpenHands
Clarify fork can run as a standalone project.
"""

